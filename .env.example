# Contact form delivery (Resend)
RESEND_API_KEY=

# Where contact form submissions land. Defaults to site.email if unset.
CONTACT_TO_EMAIL=

# From address. Must be on a verified Resend domain to deliver to arbitrary
# recipients (otherwise emails only reach the verified Resend account email).
CONTACT_FROM_EMAIL=contact@parkerfeirstein.com
